# DOCX reading
python-docx>=1.1

# Cloudflare R2 (S3-compatible) upload
boto3>=1.34
botocore>=1.34

# Neon Postgres
sqlalchemy>=2.0
psycopg[binary]>=3.1

# Microsoft Graph and the Qwen2.5 endpoint are called with the standard
# library (urllib), so no HTTP client dependency is needed.
